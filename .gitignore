# Dependencies
node_modules/

# Build output (dist/ IS committed for JS Action releases — GitHub runs dist/index.js
# directly from the pinned SHA with no build step; see action.yml runs.main)
build/
*.tsbuildinfo

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Environment
.env
.env.local
.env.*.local

# Editor / OS
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json

# Local agent config (NEVER commit — local-only)
.agents/
AGENTS.md

# Ledgerful engine state (local-only; created by `ledgerful init`)
.ledgerful/

# Temporary output
output/
*.tmp