{
  "name": "ledgerful-action",
  "version": "0.0.0",
  "description": "GitHub Action that runs the real Ledgerful engine binary in your runner over a PR diff and posts a change-risk summary.",
  "license": "SEE LICENSE AND COMMERCIAL-EXCEPTION.md",
  "author": "Ledgerful, LLC",
  "main": "dist/index.js",
  "scripts": {
    "lint": "eslint .",
    "build": "tsc --noEmit --skipLibCheck && ncc build src/index.ts -o dist --minify",
    "test": "vitest run"
  },
  "dependencies": {
    "@actions/core": "^1.11.0",
    "@actions/github": "^6.0.0",
    "@actions/tool-cache": "^2.0.1"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript-eslint": "^8.0.0",
    "eslint": "^9.0.0",
    "typescript": "^5.6.0",
    "vitest": "^2.0.0",
    "@vercel/ncc": "^0.38.0"
  },
  "engines": {
    "node": ">=24"
  }
}